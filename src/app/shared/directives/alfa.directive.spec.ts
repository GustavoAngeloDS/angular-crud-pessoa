import { AlfaDirective } from './alfa.directive';

describe('AlfaDirective', () => {
  it('should create an instance', () => {
    const directive = new AlfaDirective();
    expect(directive).toBeTruthy();
  });
});
